'use strict';

/**
 * adolescence-prevention service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::adolescence-prevention.adolescence-prevention');
